{
  "name": "claude-code-test",
  "version": "1.0.0",
  "description": "A browser-based todo app with persistence, built in a functional programming style.",
  "scripts": {
    "test": "jest --env jsdom",
    "start": "npx serve src"
  },
  "keywords": [],
  "license": "ISC",
  "devDependencies": {
    "@jest/globals": "^30.2.0",
    "jest": "^30.2.0",
    "jest-environment-jsdom": "^30.2.0"
  }
}
